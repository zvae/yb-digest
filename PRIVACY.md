# YB Digest 隐私说明

更新日期：2026-09-07。适用于当前合并版 Chrome / Edge 扩展。

本项目没有开发者后台，不含分析、广告或跟踪 SDK，不加载远程代码。

## 本地数据

- API 密钥与设置保存在本机 `chrome.storage.local`，不使用账号同步存储。
- 字幕及翻译缓存、总结、概览、笔记和问答历史保存在扩展自己的 IndexedDB。
- 缓存沿用 30 天有效期、最多 20 个视频的限制；长期总结、概览和笔记不随缓存淘汰。
- 内容脚本不能直接读取设置存储；外观通过后台消息下发。
- 备份不包含设置和密钥。卸载扩展会删除该扩展的本机存储。

## 网络请求

1. **Bilibili**：访问 `api.bilibili.com` 获取视频信息和字幕轨，访问
   `*.hdslb.com` 下载字幕。官方接口按需要携带现有 B 站登录 Cookie；不申请
   `cookies` 权限、不读取 Cookie 内容，字幕 CDN 请求不携带 Cookie。
2. **YouTube / Supadata**：内容脚本读取当前视频标题、频道和播放时间。
   获取 YouTube 字幕时向 `api.supadata.ai` 发送规范化视频链接、指定语言和
   Supadata 密钥，不发送原链接中的播放列表、分享追踪、时间戳或其他参数。
   请求不携带浏览器 Cookie，禁止 HTTP 重定向，密钥只发往 Supadata。
   仅请求原生字幕，不自动切换音频转录。Supadata 的计费及数据保留以其条款为准。
3. **用户配置的 AI 服务**：只有使用相应 AI 功能时，才发送所需的字幕、视频标题、
   选中文字、笔记正文或提问，以及 AI 密钥。可使用本机推理服务。

安装时请求 YouTube、Bilibili 及其字幕服务的特定域名权限。
自定义 AI 地址支持 HTTP 和 HTTPS，通过「保存并授权」按地址单独申请访问权限。
HTTP 不加密传输，API 密钥和请求内容可能被网络中的第三方读取；建议优先使用 HTTPS。

第三方服务如何处理数据由其各自的隐私条款决定。Supadata 和 AI 密钥是两套独立凭据，
不会互相发送到另一服务。

## 用户控制

在设置页清空 Supadata 密钥并保存可停用 YouTube 网络字幕获取，已缓存字幕仍可读取。
可删除笔记、导出学习资料备份，或在浏览器扩展管理页撤销域名权限。
关闭页内弹窗、关闭视频标签页或切换视频时，会中止该弹窗的客户端 AI 请求和后续处理，
保留已保存的学习结果。取消无法撤销第三方服务已处理的请求、已接收的数据或已产生的费用。
播放器独立笔记按钮的保存与润色不属于弹窗任务。
旧扩展与新扩展的存储彼此隔离，不会自动读取或迁移旧 YouTube 私有数据。
